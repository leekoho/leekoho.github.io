# leekoho 视觉规范

以已确认的预览为准，首页标题使用 Blog。整体参考 chanhdai.com/bookmarks 的窄栏和细线节奏，内容保留本站现有文章与个人信息。

## 品牌与文案

- 站名：Blog；浏览器标题：Blog · leekoho。
- 首页介绍以 `src/consts.ts` 的 `SITE_DESCRIPTION` 为准。
- Logo 使用已确认的 A / 折角环，源文件为 `src/assets/logo.svg`，搭配 `leekoho` 字标。保持路径比例，使用 currentColor 跟随主题。
- 导航为 Blog、About，RSS 放在页脚。文案简洁、具体，避免宣传口号和夸张修饰。

## 字体

- 中文优先使用本机苹方（PingFang SC），缺失时回落至 Noto Sans SC Variable；拉丁字母使用 Geist，代码使用 Geist Mono，代码中的中文沿用同一中文字体顺序。
- Noto Sans SC 通过 Fontsource 导入公共外部 CSS，自托管字体文件。中文声明不内联进 HTML，不使用微软雅黑，也不在 Noto 之前插入通用字体回退。
- Geist 与 Geist Mono 继续通过 Astro Fonts 自托管，仅预加载 Geist 拉丁子集。CJK 保留完整 Unicode ranges，按页面字符加载，不预加载中文字体。
- 正文使用 `text-base leading-8`，常规字重 400，标题字重 500。禁止中文使用衬线字体。

## 框架与配色

- 页面最大宽度 768px，左右 1px 边框；分区横线延伸至视口两侧。
- 浅色背景 #ffffff，正文 #202024，次要文字 #707077，边框 #e8e8ec。
- 深色背景 #111113，正文 #eeeef0，次要文字 #aaaab2，边框 #2a2a30。
- 使用连续列表、留白、细线和 `h-4` 斜线分隔带。悬停使用轻微灰底。
- 导航高 `h-16`。Logo 图形桌面 32px、手机 28px，字标桌面 20px、手机 18px。
- 首页标题 36px / 1.5，字距 .035em；介绍文字 14px / 2。
- 文章末尾使用横贯视口的斜线分隔带和细线信息网格。网格展示作者、文章发布日期和标签，桌面三列、手机两列且标签独占一行；缺失字段不占位。全站页脚显示站点链接和版权。

## 间距

- 保留语义样式名，用 `@apply` 和 Tailwind 标准尺度定义间距，不使用任意像素间距。
- 全站横向内边距统一为 `px-4 sm:px-6`（16px / 24px）；页头、页面标题、区块标题、列表、文章正文和页脚使用同一条对齐线。列表图标后的文本按图标宽度自然缩进。
- 页面介绍区使用 `py-8 sm:py-12`；内容页末尾留白使用 `h-8 sm:h-12`。
- 区块标题与资料行使用 `py-4`，文章列表行使用 `py-6`；年份和分类条使用 `py-2 leading-6`。
- 元信息与标题相隔 `mt-2`；独立内容之间优先使用 `gap-4`，图标和按钮组使用 `gap-2`，表单控件之间使用 `gap-4`。
- 正文段落、列表、代码块、引用和图片外间距使用 `my-6`；二级标题 `mt-10 mb-4`，三级标题 `mt-8 mb-4`，四级标题 `mt-6 mb-3`。标题后的正文取消上外边距，避免覆盖标题预留的间距。
- 列表项与嵌套列表使用 `my-2`；引用内段落使用 `my-4`，首尾子元素不附加外间距。callout 段落沿用自身间距，不叠加普通正文的 `my-6`。
- 正文容器使用 `pt-6 pb-8 sm:pt-8 sm:pb-12`；代码块、引用和提示块使用 `p-4`，表格单元格使用 `px-4 py-3`。
- 控件高度保留紧凑、默认、大号三级：`h-8` / `h-9` / `h-11`；对应图标按钮使用相同尺寸。触屏目标至少 44px，工具栏必要时换行。

## 文章与关于

- 文章按年份倒序归档，每行包含图标、标题、日期和标签，不显示简介。
- 列表标题使用 `text-base leading-7`，元信息使用 `text-xs leading-5`。
- 文章详情参考 https://chanhdai.com/blog/joining-claude-for-open-source-program：顶部留白后是独立工具栏，包含返回 Blog、复制正文、复制链接与相邻文章入口。
- 文章标题使用 `text-3xl sm:text-4xl leading-snug`，使用上下分区细线；真实摘要为灰色正文，阅读区沿用全站横向内边距。正文结束后显示斜线分隔带与文章信息网格，作者、日期和标签不重复展示。
- 复制正文仅从已显示的阅读区域提取文本，密码文章解锁前禁用；相邻文章遵循归档顺序，生产构建排除草稿。
- 代码与宽表格在各自容器内横向滚动。图片不超出正文。
- 关于页采用「基本信息」「在别处」「工作台」的分区列表，保留实际资料。
- 保持 Markdown、callout、密码文章、RSS、站点地图、生产环境草稿过滤等既有能力。

## 小屏幕与交互

- 使用 Tailwind `sm`（640px）断点切换桌面间距；手机横向内边距 16px，桌面 24px，页面标题手机 30px、桌面 36px。
- 列表保留小图标和箭头，文本自然换行，页面不能产生横向溢出。
- 浅色、深色、跟随系统均可选，选择持久化，并在页面绘制前设置主题。
- 保留键盘焦点、跳至正文入口、主题菜单 Escape 关闭和触屏目标尺寸。
- 尊重 prefers-reduced-motion，避免浮动卡片与持续动画。
