# leekoho 视觉规范

以已确认的预览为准，首页标题使用 Blog。整体参考 chanhdai.com/bookmarks 的窄栏和细线节奏，内容保留本站现有文章与个人信息。

## 品牌与文案

- 站名：Blog；浏览器标题：Blog · leekoho。
- 首页介绍：写给此刻，也留给以后。
- Logo 使用已确认的 A / 折角环，源文件为 `src/assets/logo.svg`，搭配 `leekoho` 字标。保持路径比例，使用 currentColor 跟随主题。
- 导航为「文章」「关于」，RSS 放在页脚。文案简洁、具体，避免宣传口号和夸张修饰。

## 字体

- 中文统一 Noto Sans SC Variable；拉丁字母使用 Geist；代码使用 Geist Mono，中文回落至 Noto Sans SC。
- 通过 Astro Fonts 自托管。字体变量依次组合为 Geist、Noto Sans SC、system-ui、sans-serif，不能在 Noto 之前插入通用 fallback。
- 仅预加载 Geist 拉丁子集。CJK 保留完整 Unicode ranges，按页面字符加载，避免预加载全部中文字体。
- 正文 16px / 1.95，常规字重 400，标题字重 500。禁止中文使用衬线字体。

## 框架与配色

- 页面最大宽度 768px，左右 1px 边框；分区横线延伸至视口两侧。
- 浅色背景 #ffffff，正文 #202024，次要文字 #707077，边框 #e8e8ec。
- 深色背景 #111113，正文 #eeeef0，次要文字 #aaaab2，边框 #2a2a30。
- 使用连续列表、留白、细线和 19px 斜线分隔带。悬停使用轻微灰底。
- 导航桌面高 66px，左右间距 24px。Logo 图形 32px，字标 20px。
- 首页标题 36px / 1.5，字距 .035em；介绍文字 14px / 2。

## 文章与关于

- 文章按年份倒序归档，每行包含图标、标题、简介、日期和标签。
- 列表标题 16px / 1.8，简介 13px / 1.95，元信息 11px。
- 文章页保留返回入口、真实标题、作者、日期、标签，正文内边距 32px。
- 代码与宽表格在各自容器内横向滚动。图片不超出正文。
- 关于页采用「基本信息」「在别处」「工作台」的分区列表，保留实际资料。
- 保持 Markdown、callout、密码文章、RSS、站点地图、生产环境草稿过滤等既有能力。

## 小屏幕与交互

- 520px 以下导航高 61px；页面横向间距约 20px，首页标题 33px，文章标题 26px。
- 列表保留小图标和箭头，文本自然换行，页面不能产生横向溢出。
- 浅色、深色、跟随系统均可选，选择持久化，并在页面绘制前设置主题。
- 保留键盘焦点、跳至正文入口、主题菜单 Escape 关闭和触屏目标尺寸。
- 尊重 prefers-reduced-motion，避免浮动卡片与持续动画。
